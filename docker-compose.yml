version: '3'

services:
  repositoryanalyticsapi:
    image: repositoryanalyticsapi
    build:
      context: .
      dockerfile: RepositoryAnalyticsApi/Dockerfile
    ports: 
      - "32771:80"
    env_file:
      - GitHubConfiguration.env

  mongodb:
    image: mongo
    ports:
      - "27017:27017"